<!DOCTYPE html>
<html>
  <% include _header %>
  <body>
    <div id='wrapper'>
      <% include _navbar %>
      <div id='page-wrapper'>
        <div class='container-fluid'>
          <!-- Page Heading -->
          <div class="row">
            <div class="col-lg-12">
              <h1 class="page-header"><%= client.name %> <small><%= client.stores.length %> stores</small></h1>
              <ol class="breadcrumb">
                <li class="active"><i class="fa fa-dashboard"></i> Stores</li>
              </ol>
            </div>
          </div>
          <!-- /.row -->
          <% include _add_store %>
          <% include _stores %>
        </div>
      </div>
    </div>
    <% include _bottom_js %>
  </body>
</html>
